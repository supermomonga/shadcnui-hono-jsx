// Generated by shadcnui-hono-jsx. DO NOT EDIT; run `bun run generate alert-dialog`.
// upstream: shadcn/ui base-rhea/alert-dialog (https://ui.shadcn.com/r/styles/base-rhea/alert-dialog.json)
// upstream-revision: sha256:59c92c9f6da929127340c71af46e90e7884313bdfab45548347c78cfdb7a8c57; shadcn-ui/ui@98a1fe67b439324ddc857f47fbdce056600a4329
// mode: generated
// Derived from shadcn/ui. Copyright (c) 2023 shadcn.
// Original additions and modifications: Copyright (c) 2026 supermomonga.
// SPDX-License-Identifier: MIT
// Full license: LICENSE-shadcnui-hono-jsx.txt at the project root.

import { cn } from "cn"
import type { Child, JSX, JSXNode } from "hono/jsx"
import {
  cloneElement,
  createContext,
  isValidElement,
  useContext,
  useId,
} from "hono/jsx"

import { Button } from "./button"

/** Props of the intrinsic element `T`, following Hono JSX conventions (`class`, no `render`/`asChild`). */
type ComponentProps<
  T extends keyof JSX.IntrinsicElements,
  Render = never,
> = JSX.IntrinsicElements[T] & {
  class?: string | undefined
  className?: never
  render?: Render
  asChild?: never
}

/** An element (or a function returning one) that replaces the default element, as in Base UI. */
type RenderProp = Child | ((props: Record<string, unknown>) => Child)

/**
 * Renders `render` in place of `element`, merging props like Base UI's
 * mergeProps: the render element's props win, classes are combined (the render
 * element's first) and styles are merged. `type` only applies to button and
 * input targets.
 */
function renderElement<Element>(
  element: Element,
  render: RenderProp | undefined
): Element {
  if (render === undefined) return element
  const own: Record<string, unknown> = {
    ...(element as unknown as JSXNode).props,
  }
  if (typeof render === "function") return render(own) as Element
  if (!isValidElement(render)) {
    throw new Error("render must be a JSX element or a function returning one")
  }
  const target = render as JSXNode
  const merged: Record<string, unknown> = { ...own }
  for (const [key, value] of Object.entries(target.props)) {
    if (key === "class" || key === "className") {
      merged.class = [value, own.class].filter(Boolean).join(" ")
    } else if (key === "style" && isObject(value) && isObject(own.style)) {
      merged.style = { ...own.style, ...value }
    } else {
      merged[key] = value
    }
  }
  if (
    typeof target.tag === "string" &&
    !["button", "input"].includes(target.tag)
  ) {
    if (!("type" in target.props)) delete merged.type
  }
  const { children, ...props } = merged
  return cloneElement(
    target,
    props,
    ...(children === undefined ? [] : [children as Child].flat())
  ) as Element
}

function isObject(value: unknown): value is Record<string, unknown> {
  return typeof value === "object" && value !== null
}

type AlertDialogRootProps = { id?: string | undefined; children?: Child }

interface AlertDialogContextValue {
  id: string
  titleId: string
  descriptionId: string
}

const AlertDialogContext = createContext<AlertDialogContextValue | null>(null)

function useAlertDialogContext(): AlertDialogContextValue {
  const context = useContext(AlertDialogContext)
  if (!context)
    throw new Error("AlertDialog parts must be rendered inside <AlertDialog>")
  return context
}

/**
 * Connects the trigger, the native `<dialog>`, its title and description.
 * Opening and closing use Invoker Commands, so no JavaScript is shipped.
 */
function AlertDialogRootElement({ id, children }: AlertDialogRootProps) {
  const generated = useId()
  const dialogId = id ?? `dialog${generated.replaceAll(":", "-")}`
  return (
    <AlertDialogContext.Provider
      value={{
        id: dialogId,
        titleId: `${dialogId}-title`,
        descriptionId: `${dialogId}-description`,
      }}
    >
      {children}
    </AlertDialogContext.Provider>
  )
}

function AlertDialogTriggerElement({
  type = "button",
  render,
  ...props
}: ComponentProps<"button", RenderProp>) {
  const { id } = useAlertDialogContext()
  return renderElement(
    <button
      type={type}
      command="show-modal"
      commandfor={id}
      aria-haspopup="dialog"
      {...props}
    />,
    render
  )
}

/** The native dialog renders in the top layer, so no portal is needed. */
function AlertDialogPortalElement({ children }: { children?: Child }) {
  return <>{children}</>
}

function AlertDialogPopupElement(props: ComponentProps<"dialog">) {
  const { id, titleId, descriptionId } = useAlertDialogContext()
  return (
    <dialog
      id={id}
      role="alertdialog"
      closedby="closerequest"
      aria-labelledby={titleId}
      aria-describedby={descriptionId}
      {...props}
    />
  )
}

function AlertDialogTitleElement(props: ComponentProps<"h2">) {
  return <h2 id={useContext(AlertDialogContext)?.titleId} {...props} />
}

function AlertDialogDescriptionElement(props: ComponentProps<"p">) {
  return <p id={useContext(AlertDialogContext)?.descriptionId} {...props} />
}

function AlertDialogCloseElement({
  type = "button",
  render,
  ...props
}: ComponentProps<"button", RenderProp>) {
  const { id } = useAlertDialogContext()
  return renderElement(
    <button type={type} command="close" commandfor={id} {...props} />,
    render
  )
}

function AlertDialog({ ...props }: AlertDialogRootProps) {
  return <AlertDialogRootElement data-slot="alert-dialog" {...props} />
}

function AlertDialogTrigger({
  ...props
}: ComponentProps<"button", RenderProp>) {
  return (
    <AlertDialogTriggerElement data-slot="alert-dialog-trigger" {...props} />
  )
}

function AlertDialogPortal({ ...props }: { children?: Child }) {
  return <AlertDialogPortalElement data-slot="alert-dialog-portal" {...props} />
}

function AlertDialogOverlay(_props: ComponentProps<"div">) {
  return null
}

function AlertDialogContent({
  class: className,
  size = "default",
  ...props
}: ComponentProps<"dialog"> & {
  size?: "default" | "sm"
}) {
  return (
    <AlertDialogPortal>
      <AlertDialogOverlay />
      <AlertDialogPopupElement
        data-slot="alert-dialog-content"
        data-size={size}
        class={cn(
          "inset-auto h-auto max-h-none w-auto max-w-none overflow-visible",
          "backdrop:bg-black/30 backdrop:duration-100 supports-backdrop-filter:backdrop:backdrop-blur-sm open:backdrop:animate-in open:backdrop:fade-in-0 not-open:backdrop:animate-out not-open:backdrop:fade-out-0",
          "group/alert-dialog-content fixed top-1/2 left-1/2 z-50 grid w-full -translate-x-1/2 -translate-y-1/2 gap-6 rounded-[min(var(--radius-4xl),24px)] bg-popover p-6 text-popover-foreground shadow-xl ring-1 ring-foreground/5 duration-100 outline-none data-[size=default]:max-w-xs data-[size=sm]:max-w-xs data-[size=default]:sm:max-w-md dark:ring-foreground/10 open:animate-in open:fade-in-0 open:zoom-in-95 not-open:animate-out not-open:fade-out-0 not-open:zoom-out-95 not-open:hidden transition-[display,overlay] transition-discrete",
          className
        )}
        {...props}
      />
    </AlertDialogPortal>
  )
}

function AlertDialogHeader({
  class: className,
  ...props
}: ComponentProps<"div">) {
  return (
    <div
      data-slot="alert-dialog-header"
      class={cn(
        "grid grid-rows-[auto_1fr] place-items-center gap-1.5 text-center has-data-[slot=alert-dialog-media]:grid-rows-[auto_auto_1fr] has-data-[slot=alert-dialog-media]:gap-x-6 sm:group-data-[size=default]/alert-dialog-content:place-items-start sm:group-data-[size=default]/alert-dialog-content:text-left sm:group-data-[size=default]/alert-dialog-content:has-data-[slot=alert-dialog-media]:grid-rows-[auto_1fr]",
        className
      )}
      {...props}
    />
  )
}

function AlertDialogFooter({
  class: className,
  ...props
}: ComponentProps<"div">) {
  return (
    <div
      data-slot="alert-dialog-footer"
      class={cn(
        "flex flex-col-reverse gap-2 group-data-[size=sm]/alert-dialog-content:grid group-data-[size=sm]/alert-dialog-content:grid-cols-2 sm:flex-row sm:justify-end",
        className
      )}
      {...props}
    />
  )
}

function AlertDialogMedia({
  class: className,
  ...props
}: ComponentProps<"div">) {
  return (
    <div
      data-slot="alert-dialog-media"
      class={cn(
        "mb-2 inline-flex size-16 items-center justify-center rounded-full bg-muted sm:group-data-[size=default]/alert-dialog-content:row-span-2 *:[svg:not([class*='size-'])]:size-8",
        className
      )}
      {...props}
    />
  )
}

function AlertDialogTitle({
  class: className,
  ...props
}: ComponentProps<"h2">) {
  return (
    <AlertDialogTitleElement
      data-slot="alert-dialog-title"
      class={cn(
        "font-heading text-lg font-medium sm:group-data-[size=default]/alert-dialog-content:group-has-data-[slot=alert-dialog-media]/alert-dialog-content:col-start-2",
        className
      )}
      {...props}
    />
  )
}

function AlertDialogDescription({
  class: className,
  ...props
}: ComponentProps<"p">) {
  return (
    <AlertDialogDescriptionElement
      data-slot="alert-dialog-description"
      class={cn(
        "text-sm text-balance text-muted-foreground md:text-pretty *:[a]:underline *:[a]:underline-offset-3 *:[a]:hover:text-foreground",
        className
      )}
      {...props}
    />
  )
}

function AlertDialogAction({
  class: className,
  ...props
}: Parameters<typeof Button>[0]) {
  return (
    <Button data-slot="alert-dialog-action" class={cn(className)} {...props} />
  )
}

function AlertDialogCancel({
  class: className,
  variant = "outline",
  size = "default",
  ...props
}: ComponentProps<"button", RenderProp> &
  Pick<Parameters<typeof Button>[0], "variant" | "size">) {
  return (
    <AlertDialogCloseElement
      data-slot="alert-dialog-cancel"
      class={cn(className)}
      render={<Button variant={variant} size={size} />}
      {...props}
    />
  )
}

export {
  AlertDialog,
  AlertDialogAction,
  AlertDialogCancel,
  AlertDialogContent,
  AlertDialogDescription,
  AlertDialogFooter,
  AlertDialogHeader,
  AlertDialogMedia,
  AlertDialogOverlay,
  AlertDialogPortal,
  AlertDialogTitle,
  AlertDialogTrigger,
}
