// Generated by shadcnui-hono-jsx. DO NOT EDIT; run `bun run generate collapsible`.
// upstream: shadcn/ui base-lyra/collapsible (https://ui.shadcn.com/r/styles/base-lyra/collapsible.json)
// upstream-revision: sha256:88ce11d67fd8d71b55cfd64130241a831841dee158348799d103cca69dff8c52; shadcn-ui/ui@98a1fe67b439324ddc857f47fbdce056600a4329
// mode: generated
// client: <script type="module" src="/shadcn/collapsible.js"> (public/shadcn/)
// Derived from shadcn/ui. Copyright (c) 2023 shadcn.
// Original additions and modifications: Copyright (c) 2026 supermomonga.
// SPDX-License-Identifier: MIT
// Full license: LICENSE-shadcnui-hono-jsx.txt at the project root.

import type { JSX, JSXNode } from "hono/jsx"
import { createContext, isValidElement, useContext, useId } from "hono/jsx"

/** Props of the intrinsic element `T`, following Hono JSX conventions (`class`, no `render`/`asChild`). */
type ComponentProps<
  T extends keyof JSX.IntrinsicElements,
  Render = never,
> = JSX.IntrinsicElements[T] & {
  class?: string | undefined
  className?: never
  render?: Render
  asChild?: never
}

type CollapsibleRootProps = ComponentProps<"details"> & {
  defaultOpen?: boolean | undefined
}

interface CollapsibleState {
  id: string
  open: boolean
  /** Whether the trigger is the first child, so the root is a native `<details>`. */
  native: boolean
}

const CollapsibleContext = createContext<CollapsibleState | null>(null)

/**
 * A native `<details>` when `<CollapsibleTrigger>` is its first child (its
 * `<summary>`; no JavaScript, and every other child is hidden while closed).
 * With the trigger elsewhere, the trigger is a button and the panel is
 * hidden while closed, toggled by the client script
 * `/shadcn/collapsible.js`, like Base UI.
 */
function CollapsibleRootElement({
  defaultOpen = false,
  open,
  children,
  ...props
}: CollapsibleRootProps) {
  const [first] = [children]
    .flat(Number.POSITIVE_INFINITY)
    .filter(
      (child) =>
        child !== null && child !== undefined && child !== false && child !== ""
    )
  const native =
    isValidElement(first) && (first as JSXNode).tag === CollapsibleTrigger
  const isOpen = open ?? defaultOpen
  const id = `collapsible${useId().replaceAll(":", "-")}`
  return (
    <CollapsibleContext.Provider value={{ id, open: isOpen, native }}>
      {native ? (
        <details open={isOpen || undefined} {...props}>
          {children}
        </details>
      ) : (
        <div
          data-collapsible=""
          data-open={isOpen ? "" : undefined}
          data-closed={isOpen ? undefined : ""}
          {...props}
        >
          {children}
        </div>
      )}
    </CollapsibleContext.Provider>
  )
}

function CollapsibleTriggerElement({
  class: className,
  ...props
}: ComponentProps<"button">) {
  const state = useContext(CollapsibleContext)
  if (!state || state.native) {
    return (
      <summary
        class={
          className
            ? `list-none [&::-webkit-details-marker]:hidden ${className}`
            : "list-none [&::-webkit-details-marker]:hidden"
        }
        {...props}
      />
    )
  }
  return (
    <button
      type="button"
      aria-expanded={state.open ? "true" : "false"}
      aria-controls={state.open ? `${state.id}-panel` : undefined}
      data-panel-open={state.open ? "" : undefined}
      data-collapsible-trigger=""
      class={className}
      {...props}
    />
  )
}

function CollapsiblePanelElement(props: ComponentProps<"div">) {
  const state = useContext(CollapsibleContext)
  if (!state || state.native) return <div {...props} />
  return (
    <div
      id={`${state.id}-panel`}
      hidden={!state.open}
      data-open={state.open ? "" : undefined}
      data-closed={state.open ? undefined : ""}
      data-collapsible-panel=""
      {...props}
    />
  )
}

function Collapsible({ ...props }: CollapsibleRootProps) {
  return <CollapsibleRootElement data-slot="collapsible" {...props} />
}

function CollapsibleTrigger({ ...props }: ComponentProps<"button">) {
  return (
    <CollapsibleTriggerElement data-slot="collapsible-trigger" {...props} />
  )
}

function CollapsibleContent({ ...props }: ComponentProps<"div">) {
  return <CollapsiblePanelElement data-slot="collapsible-content" {...props} />
}

export { Collapsible, CollapsibleContent, CollapsibleTrigger }
