// Generated by shadcnui-hono-jsx. DO NOT EDIT; run `bun run generate menubar`.
// upstream: shadcn/ui base-mira/menubar (https://ui.shadcn.com/r/styles/base-mira/menubar.json)
// upstream-revision: sha256:4377b1e6e0231e183c5d0798658539b937c1f844bcf77d416c6aeec02613e2e9; shadcn-ui/ui@98a1fe67b439324ddc857f47fbdce056600a4329
// mode: generated
// variant: rtl_menu-default-translucent
// client: <script type="module" src="/shadcn/menu.js"> (public/shadcn/)
// Derived from shadcn/ui. Copyright (c) 2023 shadcn.
// Original additions and modifications: Copyright (c) 2026 supermomonga.
// SPDX-License-Identifier: MIT
// Full license: LICENSE-shadcnui-hono-jsx.txt at the project root.
// Icons: lucide@1.48.0 (check). Copyright (c) 2026 Lucide Icons and Contributors. ISC License (see LICENSE-shadcnui-hono-jsx.txt).

import { cn } from "cn"
import type { Child, Context, JSX, JSXNode } from "hono/jsx"
import {
  cloneElement,
  createContext,
  isValidElement,
  useContext,
} from "hono/jsx"

import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuGroup,
  DropdownMenuItem,
  DropdownMenuLabel,
  DropdownMenuPortal,
  DropdownMenuRadioGroup,
  DropdownMenuSeparator,
  DropdownMenuShortcut,
  DropdownMenuSub,
  DropdownMenuSubContent,
  DropdownMenuSubTrigger,
  DropdownMenuTrigger,
} from "./dropdown-menu"

/** Props of the intrinsic element `T`, following Hono JSX conventions (`class`, no `render`/`asChild`). */
type ComponentProps<
  T extends keyof JSX.IntrinsicElements | "svg",
  Render = never,
> = (T extends keyof JSX.IntrinsicElements
  ? JSX.IntrinsicElements[T]
  : JSX.HTMLAttributes) & {
  class?: string | undefined
  className?: never
  render?: Render
  asChild?: never
}

/** An element (or a function returning one) that replaces the default element, as in Base UI. */
type RenderProp = Child | ((props: Record<string, unknown>) => Child)

/**
 * Renders `render` in place of `element`, merging props like Base UI's
 * mergeProps: the render element's props win, classes are combined (the render
 * element's first) and styles are merged. `type` only applies to button and
 * input targets.
 */
function renderElement<Element>(
  element: Element,
  render: RenderProp | undefined
): Element {
  if (render === undefined) return element
  const own: Record<string, unknown> = {
    ...(element as unknown as JSXNode).props,
  }
  if (typeof render === "function") return render(own) as Element
  if (!isValidElement(render)) {
    throw new Error("render must be a JSX element or a function returning one")
  }
  const target = render as JSXNode
  const merged: Record<string, unknown> = { ...own }
  for (const [key, value] of Object.entries(target.props)) {
    if (key === "class" || key === "className") {
      merged.class = [value, own.class].filter(Boolean).join(" ")
    } else if (key === "style" && isObject(value) && isObject(own.style)) {
      merged.style = { ...own.style, ...value }
    } else {
      merged[key] = value
    }
  }
  if (
    typeof target.tag === "string" &&
    !["button", "input"].includes(target.tag)
  ) {
    if (!("type" in target.props)) delete merged.type
  }
  const { children, ...props } = merged
  return cloneElement(
    target,
    props,
    ...(children === undefined ? [] : [children as Child].flat())
  ) as Element
}

function isObject(value: unknown): value is Record<string, unknown> {
  return typeof value === "object" && value !== null
}

type MenuItemProps = ComponentProps<"div", RenderProp> & {
  disabled?: boolean | undefined
  /** Close the menu when the item is chosen. */
  closeOnClick?: boolean | undefined
}

type MenuCheckboxItemProps = MenuItemProps & {
  checked?: boolean | undefined
  defaultChecked?: boolean | undefined
}

type MenuRadioItemProps = MenuItemProps & { value: string }

/**
 * A context shared by every generated file (the same key gives the same
 * context), so parts rendered by sibling files connect: a menubar in
 * menubar.tsx and the menus of dropdown-menu.tsx inside it.
 */
function sharedContext<T>(key: string, fallback: T): Context<T> {
  const registry = globalThis as unknown as Record<
    symbol,
    Context<T> | undefined
  >
  const symbol = Symbol.for(`shadcnui-hono-jsx:${key}`)
  registry[symbol] ??= createContext(fallback)
  return registry[symbol]
}

/** Set by a menubar: its menu triggers are menuitems, and the first one is tabbable. */
const MenubarContext = sharedContext<{ claimed: boolean } | null>(
  "menubar",
  null
)

/** Labels groups (and radio groups) by their label element. */
const MenuGroupContext = sharedContext<{
  labelId: string
  value?: string | undefined
} | null>("menu-group", null)

/** Attributes every item renders, like Base UI's. */
function menuItemAttributes(
  role: string,
  disabled: boolean | undefined,
  closeOnClick: boolean | undefined
) {
  return {
    role,
    // Focusable without the script too, so the keyboard can reach the items.
    tabindex: 0,
    "aria-disabled": disabled ? "true" : undefined,
    "data-disabled": disabled ? "" : undefined,
    "data-close-on-click":
      closeOnClick === undefined ? undefined : String(closeOnClick),
  }
}

/** Checked state attributes of checkbox and radio items. */
function menuCheckedAttributes(checked: boolean) {
  return {
    "aria-checked": checked ? "true" : "false",
    "data-checked": checked ? "" : undefined,
    "data-unchecked": checked ? undefined : "",
  }
}

function MenuCheckboxItemElement({
  checked,
  defaultChecked = false,
  disabled,
  closeOnClick,
  render,
  ...props
}: MenuCheckboxItemProps) {
  return renderElement(
    <div
      {...menuItemAttributes("menuitemcheckbox", disabled, closeOnClick)}
      {...menuCheckedAttributes(checked ?? defaultChecked)}
      {...props}
    />,
    render
  )
}

function MenuRadioItemElement({
  value,
  disabled,
  closeOnClick,
  render,
  ...props
}: MenuRadioItemProps) {
  const group = useContext(MenuGroupContext)
  return renderElement(
    <div
      {...menuItemAttributes("menuitemradio", disabled, closeOnClick)}
      {...menuCheckedAttributes(group?.value === value)}
      data-value={value}
      {...props}
    />,
    render
  )
}

/** Shown while the item is checked (Base UI mounts it only then). */
function MenuItemIndicatorElement(props: ComponentProps<"span">) {
  return (
    <span
      aria-hidden="true"
      class="in-[[aria-checked=false]]:hidden"
      {...props}
    />
  )
}

type MenubarRootProps = ComponentProps<"div"> & {
  orientation?: "horizontal" | "vertical" | undefined
  /** Wrap focus from the last menu to the first with the arrow keys. */
  loopFocus?: boolean | undefined
}

/** A bar of menus: their triggers are menuitems, moved between with the arrow keys. */
function MenubarRootElement({
  orientation = "horizontal",
  loopFocus,
  ...props
}: MenubarRootProps) {
  return (
    <MenubarContext.Provider value={{ claimed: false }}>
      <div
        role="menubar"
        aria-orientation={orientation}
        data-orientation={orientation}
        data-loop-focus={loopFocus === false ? "false" : undefined}
        {...props}
      />
    </MenubarContext.Provider>
  )
}

function Menubar({ class: className, ...props }: MenubarRootProps) {
  return (
    <MenubarRootElement
      data-slot="menubar"
      class={cn("flex h-9 items-center rounded-lg border p-1", className)}
      {...props}
    />
  )
}

function MenubarMenu({ ...props }: Parameters<typeof DropdownMenu>[0]) {
  return <DropdownMenu data-slot="menubar-menu" {...props} />
}

function MenubarGroup({ ...props }: Parameters<typeof DropdownMenuGroup>[0]) {
  return <DropdownMenuGroup data-slot="menubar-group" {...props} />
}

function MenubarPortal({ ...props }: Parameters<typeof DropdownMenuPortal>[0]) {
  return <DropdownMenuPortal data-slot="menubar-portal" {...props} />
}

function MenubarTrigger({
  class: className,
  ...props
}: Parameters<typeof DropdownMenuTrigger>[0]) {
  return (
    <DropdownMenuTrigger
      data-slot="menubar-trigger"
      class={cn(
        "flex items-center rounded-[calc(var(--radius-md)-2px)] px-2 py-[calc(--spacing(0.85))] text-xs/relaxed font-medium outline-hidden select-none hover:bg-muted aria-expanded:bg-muted",
        className
      )}
      {...props}
    />
  )
}

function MenubarContent({
  class: className,
  align = "start",
  alignOffset = -4,
  sideOffset = 8,
  ...props
}: Parameters<typeof DropdownMenuContent>[0]) {
  return (
    <DropdownMenuContent
      data-slot="menubar-content"
      align={align}
      alignOffset={alignOffset}
      sideOffset={sideOffset}
      class={cn(
        "min-w-32 rounded-lg p-1 text-popover-foreground shadow-md ring-1 ring-foreground/10 duration-100 data-[side=bottom]:slide-in-from-top-2 data-[side=inline-end]:slide-in-from-start-2 data-[side=inline-start]:slide-in-from-end-2 data-[side=left]:slide-in-from-right-2 data-[side=right]:slide-in-from-left-2 data-[side=top]:slide-in-from-bottom-2 data-open:animate-in data-open:fade-in-0 data-open:zoom-in-95 animate-none! relative bg-popover/70 before:pointer-events-none before:absolute before:inset-0 before:-z-1 before:rounded-[inherit] before:backdrop-blur-2xl before:backdrop-saturate-150 **:data-[slot$=-item]:focus:bg-foreground/10 **:data-[slot$=-item]:data-highlighted:bg-foreground/10 **:data-[slot$=-separator]:bg-foreground/5 **:data-[slot$=-trigger]:focus:bg-foreground/10 **:data-[slot$=-trigger]:aria-expanded:bg-foreground/10! **:data-[variant=destructive]:focus:bg-foreground/10! **:data-[variant=destructive]:text-accent-foreground! **:data-[variant=destructive]:**:text-accent-foreground!",
        className
      )}
      {...props}
    />
  )
}

function MenubarItem({
  class: className,
  inset,
  variant = "default",
  ...props
}: Parameters<typeof DropdownMenuItem>[0]) {
  return (
    <DropdownMenuItem
      data-slot="menubar-item"
      data-inset={inset}
      data-variant={variant}
      class={cn(
        "group/menubar-item min-h-7 gap-2 rounded-md px-2 py-1 text-xs/relaxed focus:bg-accent focus:text-accent-foreground not-data-[variant=destructive]:focus:**:text-accent-foreground data-inset:ps-7.5 data-[variant=destructive]:text-destructive data-[variant=destructive]:focus:bg-destructive/10 data-[variant=destructive]:focus:text-destructive dark:data-[variant=destructive]:focus:bg-destructive/20 data-disabled:opacity-50 [&_svg:not([class*='size-'])]:size-3.5 data-[variant=destructive]:*:[svg]:text-destructive!",
        className
      )}
      {...props}
    />
  )
}

function MenubarCheckboxItem({
  class: className,
  children,
  checked,
  inset,
  ...props
}: MenuCheckboxItemProps & {
  inset?: boolean
}) {
  return (
    <MenuCheckboxItemElement
      data-slot="menubar-checkbox-item"
      data-inset={inset}
      class={cn(
        "relative flex min-h-7 cursor-default items-center gap-2 rounded-md py-1.5 pe-2 ps-7.5 text-xs outline-hidden select-none focus:bg-accent focus:text-accent-foreground focus:**:text-accent-foreground data-inset:ps-7.5 data-disabled:pointer-events-none data-disabled:opacity-50 [&_svg]:pointer-events-none [&_svg]:shrink-0",
        className
      )}
      checked={checked}
      {...props}
    >
      <span class="pointer-events-none absolute start-2 flex size-4 items-center justify-center [&_svg:not([class*='size-'])]:size-4">
        <MenuItemIndicatorElement>
          <CheckIcon />
        </MenuItemIndicatorElement>
      </span>
      {children}
    </MenuCheckboxItemElement>
  )
}

function MenubarRadioGroup({
  ...props
}: Parameters<typeof DropdownMenuRadioGroup>[0]) {
  return <DropdownMenuRadioGroup data-slot="menubar-radio-group" {...props} />
}

function MenubarRadioItem({
  class: className,
  children,
  inset,
  ...props
}: MenuRadioItemProps & {
  inset?: boolean
}) {
  return (
    <MenuRadioItemElement
      data-slot="menubar-radio-item"
      data-inset={inset}
      class={cn(
        "relative flex min-h-7 cursor-default items-center gap-2 rounded-md py-1.5 pe-2 ps-7.5 text-xs outline-hidden select-none focus:bg-accent focus:text-accent-foreground focus:**:text-accent-foreground data-inset:ps-7.5 data-disabled:pointer-events-none data-disabled:opacity-50 [&_svg]:pointer-events-none [&_svg]:shrink-0 [&_svg:not([class*='size-'])]:size-3.5",
        className
      )}
      {...props}
    >
      <span class="pointer-events-none absolute start-2 flex size-4 items-center justify-center [&_svg:not([class*='size-'])]:size-4">
        <MenuItemIndicatorElement>
          <CheckIcon />
        </MenuItemIndicatorElement>
      </span>
      {children}
    </MenuRadioItemElement>
  )
}

function MenubarLabel({
  class: className,
  inset,
  ...props
}: Parameters<typeof DropdownMenuLabel>[0] & {
  inset?: boolean
}) {
  return (
    <DropdownMenuLabel
      data-slot="menubar-label"
      data-inset={inset}
      class={cn(
        "px-2 py-1.5 text-xs text-muted-foreground data-inset:ps-7.5",
        className
      )}
      {...props}
    />
  )
}

function MenubarSeparator({
  class: className,
  ...props
}: Parameters<typeof DropdownMenuSeparator>[0]) {
  return (
    <DropdownMenuSeparator
      data-slot="menubar-separator"
      class={cn("-mx-1 my-1 h-px bg-border/50", className)}
      {...props}
    />
  )
}

function MenubarShortcut({
  class: className,
  ...props
}: Parameters<typeof DropdownMenuShortcut>[0]) {
  return (
    <DropdownMenuShortcut
      data-slot="menubar-shortcut"
      class={cn(
        "ms-auto text-[0.625rem] tracking-widest text-muted-foreground group-focus/menubar-item:text-accent-foreground",
        className
      )}
      {...props}
    />
  )
}

function MenubarSub({ ...props }: Parameters<typeof DropdownMenuSub>[0]) {
  return <DropdownMenuSub data-slot="menubar-sub" {...props} />
}

function MenubarSubTrigger({
  class: className,
  inset,
  ...props
}: Parameters<typeof DropdownMenuSubTrigger>[0] & {
  inset?: boolean
}) {
  return (
    <DropdownMenuSubTrigger
      data-slot="menubar-sub-trigger"
      data-inset={inset}
      class={cn(
        "min-h-7 gap-2 rounded-md px-2 py-1 text-xs focus:bg-accent focus:text-accent-foreground not-data-[variant=destructive]:focus:**:text-accent-foreground data-inset:ps-7.5 data-open:bg-accent data-open:text-accent-foreground [&_svg:not([class*='size-'])]:size-3.5",
        className
      )}
      {...props}
    />
  )
}

function MenubarSubContent({
  class: className,
  ...props
}: Parameters<typeof DropdownMenuSubContent>[0]) {
  return (
    <DropdownMenuSubContent
      data-slot="menubar-sub-content"
      class={cn(
        "min-w-32 rounded-lg p-1 text-popover-foreground shadow-md ring-1 ring-foreground/10 duration-100 data-[side=bottom]:slide-in-from-top-2 data-[side=left]:slide-in-from-right-2 data-[side=right]:slide-in-from-left-2 data-[side=top]:slide-in-from-bottom-2 data-open:animate-in data-open:fade-in-0 data-open:zoom-in-95 data-closed:animate-out data-closed:fade-out-0 data-closed:zoom-out-95 animate-none! relative bg-popover/70 before:pointer-events-none before:absolute before:inset-0 before:-z-1 before:rounded-[inherit] before:backdrop-blur-2xl before:backdrop-saturate-150 **:data-[slot$=-item]:focus:bg-foreground/10 **:data-[slot$=-item]:data-highlighted:bg-foreground/10 **:data-[slot$=-separator]:bg-foreground/5 **:data-[slot$=-trigger]:focus:bg-foreground/10 **:data-[slot$=-trigger]:aria-expanded:bg-foreground/10! **:data-[variant=destructive]:focus:bg-foreground/10! **:data-[variant=destructive]:text-accent-foreground! **:data-[variant=destructive]:**:text-accent-foreground!",
        className
      )}
      {...props}
    />
  )
}
// icon-helper
/** Like lucide-react, icons are hidden from assistive technology unless labelled. */
function hasA11yProp(props: object): boolean {
  return Object.keys(props).some(
    (key) => key.startsWith("aria-") || key === "role" || key === "title"
  )
}

// icon: {"lucide":"CheckIcon","tabler":"IconCheck","hugeicons":"Tick02Icon","phosphor":"CheckIcon","remixicon":"RiCheckLine"}
/** Lucide `check` icon, inlined (ISC License, see LICENSE-shadcnui-hono-jsx.txt). */
function CheckIcon({
  class: className,
  children,
  ...props
}: ComponentProps<"svg">) {
  return (
    <svg
      xmlns="http://www.w3.org/2000/svg"
      width="24"
      height="24"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      stroke-width="2"
      stroke-linecap="round"
      stroke-linejoin="round"
      class={cn("lucide lucide-check", className)}
      aria-hidden={children || hasA11yProp(props) ? undefined : "true"}
      {...props}
    >
      <path d="M20 6 9 17l-5-5" />
      {children}
    </svg>
  )
}

export {
  Menubar,
  MenubarCheckboxItem,
  MenubarContent,
  MenubarGroup,
  MenubarItem,
  MenubarLabel,
  MenubarMenu,
  MenubarPortal,
  MenubarRadioGroup,
  MenubarRadioItem,
  MenubarSeparator,
  MenubarShortcut,
  MenubarSub,
  MenubarSubContent,
  MenubarSubTrigger,
  MenubarTrigger,
}
