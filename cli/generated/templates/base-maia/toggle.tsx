// Generated by shadcnui-hono-jsx. DO NOT EDIT; run `bun run generate toggle`.
// upstream: shadcn/ui base-maia/toggle (https://ui.shadcn.com/r/styles/base-maia/toggle.json)
// upstream-revision: sha256:8494500ccbeea082b88fb5035dedf404134927398185b89aded6ca1ab9079426; shadcn-ui/ui@98a1fe67b439324ddc857f47fbdce056600a4329
// mode: generated
// Derived from shadcn/ui. Copyright (c) 2023 shadcn.
// Original additions and modifications: Copyright (c) 2026 supermomonga.
// SPDX-License-Identifier: MIT
// Full license: LICENSE-shadcnui-hono-jsx.txt at the project root.

import { cva, type VariantProps } from "class-variance-authority"
import { cn } from "cn"
import type { JSX } from "hono/jsx"
import { createContext, useContext } from "hono/jsx"

/** Props of the intrinsic element `T`, following Hono JSX conventions (`class`, no `render`/`asChild`). */
type ComponentProps<
  T extends keyof JSX.IntrinsicElements,
  Render = never,
> = JSX.IntrinsicElements[T] & {
  class?: string | undefined
  className?: never
  render?: Render
  asChild?: never
}

const CONTROL_INPUT_PROPS = new Set([
  "id",
  "name",
  "value",
  "disabled",
  "required",
  "form",
  "autofocus",
])

/** Splits props between the native input (form and ARIA attributes) and the styled root. */
function splitControlProps(
  props: Record<string, unknown>
): [Record<string, unknown>, Record<string, unknown>] {
  const input: Record<string, unknown> = {}
  const root: Record<string, unknown> = {}
  for (const [key, value] of Object.entries(props)) {
    if (CONTROL_INPUT_PROPS.has(key) || key.startsWith("aria-"))
      input[key] = value
    else root[key] = value
  }
  return [input, root]
}

type ToggleRootProps = ComponentProps<"label"> & {
  pressed?: boolean | undefined
  defaultPressed?: boolean | undefined
  disabled?: boolean | undefined
  name?: string | undefined
  value?: string | undefined
  form?: string | undefined
}

/** Set by a toggle group: its items are radios (or checkboxes with `multiple`) sharing a name. */
interface ToggleGroupItemContextValue {
  name: string
  multiple: boolean
  values: readonly string[]
  disabled: boolean
}

const ToggleGroupItemContext =
  createContext<ToggleGroupItemContextValue | null>(null)

/** A label around a visually hidden checkbox: pressing toggles it without JavaScript. */
function ToggleRootElement({
  pressed,
  defaultPressed,
  value,
  disabled,
  children,
  ...props
}: ToggleRootProps) {
  const group = useContext(ToggleGroupItemContext)
  const [input, root] = splitControlProps(props)
  const isPressed = group
    ? value !== undefined && group.values.includes(value)
    : (pressed ?? defaultPressed ?? false)
  return (
    <label {...root}>
      <input
        type={group && !group.multiple ? "radio" : "checkbox"}
        name={group?.name}
        value={value}
        checked={isPressed || undefined}
        disabled={group?.disabled || disabled || undefined}
        class="peer sr-only"
        {...input}
      />
      {children}
    </label>
  )
}

const toggleVariants = cva(
  "group/toggle inline-flex items-center justify-center gap-1 rounded-4xl text-sm font-medium whitespace-nowrap transition-colors outline-none hover:bg-muted hover:text-foreground has-focus-visible:border-ring has-focus-visible:ring-[3px] has-focus-visible:ring-ring/50 has-disabled:pointer-events-none has-disabled:opacity-50 has-aria-invalid:border-destructive has-aria-invalid:ring-destructive/20 has-checked:bg-muted dark:has-aria-invalid:ring-destructive/40 [&_svg]:pointer-events-none [&_svg]:shrink-0 [&_svg:not([class*='size-'])]:size-4",
  {
    variants: {
      variant: {
        default: "bg-transparent",
        outline: "border border-input bg-transparent hover:bg-muted",
      },
      size: {
        default:
          "h-9 min-w-9 px-3 has-data-[icon=inline-end]:pr-2.5 has-data-[icon=inline-start]:pl-2.5",
        sm: "h-8 min-w-8 px-3 has-data-[icon=inline-end]:pr-2 has-data-[icon=inline-start]:pl-2",
        lg: "h-10 min-w-10 px-4 has-data-[icon=inline-end]:pr-3 has-data-[icon=inline-start]:pl-3",
      },
    },
    defaultVariants: {
      variant: "default",
      size: "default",
    },
  }
)

function Toggle({
  class: className,
  variant = "default",
  size = "default",
  ...props
}: ToggleRootProps & VariantProps<typeof toggleVariants>) {
  return (
    <ToggleRootElement
      data-slot="toggle"
      class={cn(toggleVariants({ variant, size, className }))}
      {...props}
    />
  )
}

export { Toggle, toggleVariants }
