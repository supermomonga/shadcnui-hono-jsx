// Generated by shadcnui-hono-jsx from lite/input-otp-lite.tsx. DO NOT EDIT; run `bun run generate input-otp-lite`.
// lite: a hand-written alternative that approximates shadcn/ui without JavaScript; not a port of upstream code.
// variant: rtl
// based-on: shadcn/ui base-nova/input-otp (https://ui.shadcn.com/r/styles/base-nova/input-otp.json) sha256:54f6dd46b5a450592778d4e3b238b92bfe5f9062b6be5ea6a28ae573a84d4d4a
// Derived from shadcn/ui. Copyright (c) 2023 shadcn.
// Original additions and modifications: Copyright (c) 2026 supermomonga.
// SPDX-License-Identifier: MIT
// Full license: LICENSE-shadcnui-hono-jsx.txt at the project root.

import { cn } from "cn"
import type { CSSProperties, JSX } from "hono/jsx"

/** Props of the intrinsic element `T`, following Hono JSX conventions (`class`, no `render`/`asChild`). */
type ComponentProps<
  T extends keyof JSX.IntrinsicElements,
  Render = never,
> = JSX.IntrinsicElements[T] & {
  class?: string | undefined
  className?: never
  render?: Render
  asChild?: never
}

/**
 * A lite alternative to shadcn/ui's InputOTP that needs no JavaScript, in the
 * way of daisyUI's OTP: one native text input lies over the slot boxes, its
 * characters spaced to one per box. The browser handles typing, pasting,
 * one-time-code autofill and form validation. The whole group is highlighted
 * while focused (upstream highlights the active slot).
 *
 * `lite:<key>` class tokens are computed per style from upstream's InputOTP,
 * InputOTPGroup and InputOTPSlot (inputOtpClasses in generator/src/lite.ts).
 */
function InputOTPLite({
  maxLength = 6,
  class: className,
  containerClassName,
  style,
  ...props
}: ComponentProps<"input"> & {
  /** Number of characters, and of slots. */
  maxLength?: number
  containerClassName?: string
}) {
  return (
    <div
      data-slot="input-otp-lite"
      class={cn(
        "group/input-otp relative w-fit flex items-center has-disabled:opacity-50 rounded-lg has-aria-invalid:ring-3 has-aria-invalid:ring-destructive/20 dark:has-aria-invalid:ring-destructive/40 has-focus-visible:ring-3 has-focus-visible:ring-ring/50",
        containerClassName
      )}
      style={
        {
          "--input-otp-length": String(maxLength),
        } as CSSProperties
      }
    >
      <div
        aria-hidden="true"
        data-slot="input-otp-group"
        class="flex items-center rounded-lg"
      >
        {Array.from({ length: maxLength }, (_, index) => (
          <div
            key={index}
            data-slot="input-otp-slot"
            class="relative flex size-8 items-center justify-center border-y border-e border-input text-sm transition-all outline-none first:rounded-s-lg first:border-s last:rounded-e-lg group-has-aria-invalid/input-otp:border-destructive group-has-focus-visible/input-otp:border-ring dark:bg-input/30"
          />
        ))}
      </div>
      {/*
        Clips the input's spare width (and the caret after the last slot).
        Tabular digits are 1ch wide: the spacing and padding center one per
        slot, and pb-px lines the text up with upstream's slots.
      */}
      <div class="absolute inset-0 overflow-hidden rounded-lg">
        <input
          data-slot="input-otp-lite-input"
          type="text"
          inputmode="numeric"
          autocomplete="one-time-code"
          spellcheck={false}
          maxlength={maxLength}
          minlength={maxLength}
          class={cn(
            "absolute top-0 start-0 h-full w-[calc((var(--input-otp-length)+1)*--spacing(8))] border-0 bg-transparent pb-px ps-[calc((--spacing(8)-1ch)/2)] text-sm tracking-[calc(--spacing(8)-1ch)] text-foreground tabular-nums outline-none disabled:cursor-not-allowed rtl:[unicode-bidi:bidi-override] rtl:translate-x-[calc(--spacing(8)-1ch)]",
            className
          )}
          style={style}
          {...props}
        />
      </div>
    </div>
  )
}

export { InputOTPLite }
