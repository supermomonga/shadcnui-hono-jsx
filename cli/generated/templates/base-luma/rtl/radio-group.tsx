// Generated by shadcnui-hono-jsx. DO NOT EDIT; run `bun run generate radio-group`.
// upstream: shadcn/ui base-luma/radio-group (https://ui.shadcn.com/r/styles/base-luma/radio-group.json)
// upstream-revision: sha256:61f8fcc2d72baa47661d3544c990af82879ce8afbe50c046e47e592fb9b43728; shadcn-ui/ui@98a1fe67b439324ddc857f47fbdce056600a4329
// mode: generated
// variant: rtl
// Derived from shadcn/ui. Copyright (c) 2023 shadcn.
// Original additions and modifications: Copyright (c) 2026 supermomonga.
// SPDX-License-Identifier: MIT
// Full license: LICENSE-shadcnui-hono-jsx.txt at the project root.

import { cn } from "cn"
import type { JSX } from "hono/jsx"
import { createContext, useContext, useId } from "hono/jsx"

/** Props of the intrinsic element `T`, following Hono JSX conventions (`class`, no `render`/`asChild`). */
type ComponentProps<
  T extends keyof JSX.IntrinsicElements,
  Render = never,
> = JSX.IntrinsicElements[T] & {
  class?: string | undefined
  className?: never
  render?: Render
  asChild?: never
}

type RadioGroupRootProps = ComponentProps<"div"> & {
  name?: string | undefined
  /** Value of the radio that is selected on load. */
  defaultValue?: string | undefined
  value?: string | undefined
  disabled?: boolean | undefined
  required?: boolean | undefined
}

interface RadioGroupContextValue {
  name: string | undefined
  value: string | undefined
  disabled: boolean
  required: boolean
}

const RadioGroupContext = createContext<RadioGroupContextValue>({
  name: undefined,
  value: undefined,
  disabled: false,
  required: false,
})

/** Radios in the group share a `name`, so the browser keeps one selected. */
function RadioGroupRootElement({
  name,
  defaultValue,
  value,
  disabled = false,
  required = false,
  ...props
}: RadioGroupRootProps) {
  const id = useId().replaceAll(":", "-")
  return (
    <RadioGroupContext.Provider
      value={{
        name: name ?? `radio-group${id}`,
        value: value ?? defaultValue,
        disabled,
        required,
      }}
    >
      <div
        role="radiogroup"
        aria-required={required ? "true" : undefined}
        {...props}
      />
    </RadioGroupContext.Provider>
  )
}
const CONTROL_INPUT_PROPS = new Set([
  "id",
  "name",
  "value",
  "disabled",
  "required",
  "form",
  "autofocus",
])

/** Splits props between the native input (form and ARIA attributes) and the styled root. */
function splitControlProps(
  props: Record<string, unknown>
): [Record<string, unknown>, Record<string, unknown>] {
  const input: Record<string, unknown> = {}
  const root: Record<string, unknown> = {}
  for (const [key, value] of Object.entries(props)) {
    if (CONTROL_INPUT_PROPS.has(key) || key.startsWith("aria-"))
      input[key] = value
    else root[key] = value
  }
  return [input, root]
}

type RadioRootProps = ComponentProps<"span"> & {
  value: string
  disabled?: boolean | undefined
}

/** The styled root with a native radio of the enclosing group inside, then the indicator. */
function RadioRootElement({
  value,
  disabled,
  children,
  ...props
}: RadioRootProps) {
  const group = useContext(RadioGroupContext)
  const [input, root] = splitControlProps(props)
  return (
    <span {...root}>
      <input
        type="radio"
        name={group.name}
        value={value}
        checked={group.value === value || undefined}
        disabled={group.disabled || disabled || undefined}
        required={group.required || undefined}
        class="peer absolute -inset-x-3 -inset-y-2 m-0 appearance-none outline-none"
        {...input}
      />
      {children}
    </span>
  )
}

function RadioGroup({ class: className, ...props }: RadioGroupRootProps) {
  return (
    <RadioGroupRootElement
      data-slot="radio-group"
      class={cn("grid w-full gap-3", className)}
      {...props}
    />
  )
}

function RadioGroupItem({ class: className, ...props }: RadioRootProps) {
  return (
    <RadioRootElement
      data-slot="radio-group-item"
      class={cn(
        "group/radio-group-item peer relative flex aspect-square size-4 shrink-0 rounded-full border border-transparent bg-input/90 outline-none group-has-[:focus-visible]/field-label:border-transparent group-has-[:focus-visible]/field-label:ring-0 has-focus-visible:border-ring has-focus-visible:ring-3 has-focus-visible:ring-ring/30 disabled:cursor-not-allowed disabled:opacity-50 has-aria-invalid:border-destructive has-aria-invalid:ring-3 has-aria-invalid:ring-destructive/20 dark:has-aria-invalid:border-destructive/50 dark:has-aria-invalid:ring-destructive/40 has-checked:bg-primary has-checked:text-primary-foreground dark:has-checked:bg-primary",
        className
      )}
      {...props}
    >
      <span
        data-slot="radio-group-indicator"
        class="flex size-4 items-center justify-center pointer-events-none peer-not-checked:hidden"
      >
        <span class="absolute top-1/2 start-1/2 size-2 -translate-x-1/2 rtl:translate-x-1/2 -translate-y-1/2 rounded-full bg-primary-foreground dark:size-2.5" />
      </span>
    </RadioRootElement>
  )
}

export { RadioGroup, RadioGroupItem }
